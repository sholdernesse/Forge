# Coach prompts

LLM prompt templates will live here once the deterministic recommendation layer is stable. The model layer should explain and converse about service-owned decisions rather than silently inventing health decisions in prompt text.
