# Forge AI packages

- `coach/` exposes stable coaching use cases to applications.
- `recommendation-engine/` owns deterministic, explainable decision rules.

Generative model integrations should sit behind these domain contracts rather than replacing them.
